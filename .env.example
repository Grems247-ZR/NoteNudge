# ─── 本地 Express（server.js）与 Netlify Functions 共用逻辑：服务端密钥 ───
# Netlify 后台：Site settings → Environment variables → 添加同名变量
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# ─── 仅 Vite 构建注入到浏览器（import.meta.env.VITE_*）────────────────────
# Netlify 后台变量名必须带 VITE_ 前缀才会进前端包；不要把真实 sk- 填在这里
# VITE_DEEPSEEK_API_KEY=

# 兼容：若本地 .env 里暂时只配了上一行，server.js 仍会尝试读取 VITE_DEEPSEEK_API_KEY
